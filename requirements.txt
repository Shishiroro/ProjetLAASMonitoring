#pip install -r requirements.txt

# Core pipeline
numpy
pyproj
pyyaml
opencv-python
scikit-spatial
scipy>=1.11
z3-solver
timezonefinder
pytz

# X-Plane capture
mss
Pillow

# YOLOv8 + evaluation
ultralytics
torch
pandas
matplotlib

# Windows only (gestion fenetre X-Plane, DPI-aware)
pywin32; sys_platform == "win32"

# Linux only (gestion fenetre X-Plane, sans droits systeme)
python-xlib; sys_platform == "linux"
